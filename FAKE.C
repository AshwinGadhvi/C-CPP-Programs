#include<stdio.h>
#include<conio.h>

void main()
{
	int i,n,multiply;
	clrscr();
	printf ("how many number");
	scanf ("%d",&n);
	for (i=1;i<=n;i++)
	{
		printf ("%d*",i);
	}
	printf ("%d is",multiply);
	getch();
}