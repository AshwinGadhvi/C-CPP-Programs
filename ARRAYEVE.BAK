#include<stdio.h>
#include<conio.h>

void main()
{
	int i,a[10];
	clrscr();
	printf ("enter the elements of array");
	for(i=1;i<10;i++)
	{
		printf ("a[%d]=",i);
		scanf ("%d",&a[i]);
	}
	for(i=1;i<10;i++)
	{
		if (a[i]%2==0)
		{
			printf ("\na[i]",i);
		}
	}
	getch();
}