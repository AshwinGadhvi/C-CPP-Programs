#include<stdio.h>
#include<conio.h>

void main()
{
	int i,n,a[100],find,flag;
	clrscr();
	printf ("Enter n::");
	scanf ("%d",&n);
	printf ("Enter %d elements\n",n);
	for(i=0;i<n;i++)
	{
		printf ("a[%d]=",i);
		scanf ("%d",a[i]);
	}
	printf ("Enter element to find::");
	scanf ("%d",&find);
	for(i=0;i<n;i++)
	{
		if(a[i]==find)
		{
			flag=1;
			printf ("Element found at a[%d] location::",i);																							   );
		}
	}
	if(flag==0)
	{
		printf ("Element not found::");
	}
	getch();
}