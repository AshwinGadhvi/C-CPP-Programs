#include<stdio.h>
#include<conio.h>

void main()
{
	int days,year,month;
	clrscr();
	printf ("enter the days");
	scanf ("%d",&days);
	if(days==365)
	{
		printf ("1 year");
	}
	if(days==28)
	{
		printf ("month");
	}
	if(days==31);
	{
		printf ("month");
	}
	getch();
}