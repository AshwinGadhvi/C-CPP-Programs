#include<iostream.h>
#include<conio.h>

class start
{
	private:
		char a[20];
		int i,c;
	public:
		void get_data()
		{
		     cout<<"Enter string : ";
		     cin>>a;
		}
		void length()
		{
			for(i=0;a[i]!=NULL;i++)
			{
				c++;
			}
			cout<<"Length of string: "<<c<<endl;
		}
};

void main()
{
	start s;
	clrscr();
	s.get_data();
	s.length();
	getch();
}