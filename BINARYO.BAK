#include<iostream.h>
#include<conio.h>
void main()
{
	while(low<=high)
	{
		mid=(low + high)/2;
		if(a[mid]>x)
		{
			high=mid-1;
		}
		else if(a[mid]<x)
		{
			low=mid+1;
		}
		else
		{
			flag=1;
			printf ("element found at %d",(mid+1));
			break;
		}


	}
	if(flag==0)
	{
		printf ("element not found::");
	}
	getch();
}