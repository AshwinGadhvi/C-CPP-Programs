#include<iostream.h>
#include<conio.h>

void main()
{
	int number,i,flag=0;
	clrscr();
	cout<<"Enter Number : ";
	cin>>number;
	for(i=2;i<number;i++)
	{
		if(number%i==0)
		{
			cout<<"Number is composite : ";
			flag=0;
			break;
		}
		else
		{
			flag=1;
		}
	}
	if(flag==1)
	{
		cout<<Number<<" is Prime!!";
	}
	if(flag==0)
	{
		cout<<number<<" is composite!!";
	}
	getch();
}