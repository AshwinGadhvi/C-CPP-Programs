#include<iostream.h>
#include<conio.h>
#include<stdio.h>

struct invent
{
	char *name;
	int number;
	float price;
	void getdata()
	{
		cout<<"Enter name : ";
		gets(name);
		cout<<"Enter number : ";
		cin>>number;
		cout<<"Enter price : ";
		cin>>price;
	}
	void putdata()
	{
		cout<<"Name is : "<<name<<endl;
		cout<<"Number is : "<<number<<endl;
		cout<<"Price is : "<<price<<endl;
	}
};

void main()
{
	struct invent *ptr;
	int n,a[20],i;
	clrscr();
	cout<<"Enter n : "<<endl;
	cin>>n;
	for(i=0;i<n;i++)
	{
		ptr->getdata();
		ptr++;
	}
	cout<<"Entered data is : "<<endl;
	for(i=0;i<n;i++)
	{
		ptr->putdata();
		ptr++;
	}
	getch();
}
