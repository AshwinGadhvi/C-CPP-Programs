#include<stdio.h>
#include<conio.h>

void main()
{
	int i,n,sum=0;prod=1;
	clrscr();
	printf ("enter the value of n");
	scanf ("%d",&n);
	for (i=1;i<=n;i++)
	{
		sum=i*1;
		prod=sum*-1;
	}
	printf ("%d",i);
}