#include<iostream.h>
#include<conio.h>

void main()
{
	int number,i,flag,j;
	clrscr();
	cout<<"Enter number : ";
	cin>>number;
	for(i=1;i<=number;i++)
	{
		flag=0;
		for(j=2;j<=i;j++)
		{
			if(i%j!=0)
			{
				flag++;
			}
		}
		if(flag==2)
		{
			cout<<i<<endl;
		}
	}
	getch();
}


